/** URLs y flags compartidos por el service worker. */
// Replace with your own Google Apps Script Web App URL to enable usage telemetry (optional).
export const USAGE_LOG_ENDPOINT = '';

export const DEBUG_LOGS = false;

export const UPDATE_INFO_URL = 'https://salesforceorgcompare.web.app/version.json';
export const UPDATE_INFO_TTL_MS = 10 * 60 * 1000;
